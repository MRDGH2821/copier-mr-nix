# AI Work Log - 2026-08-30

## 2026-08-30T08:29:33+10:00

### Prompt

> /simplify the entire codebase

### Prompter

MRDGH2821

### Model

Claude Sonnet 5 via Claude Code

### Actions

- Reviewed the whole repository for simplification opportunities (`nix/` and
  `template/nix/` flake modules, `flake.nix`, Copier config, linter / CI /
  tooling config).
- `nix/checks/pre-commit-check.nix`: dropped redundant `"${...}"` wrapping
  around `lib.getExe` for the `cocogitto` and `ls-lint` hook `entry` values,
  matching the un-wrapped `betterleaks` hook.

## 2026-08-30T08:55:00+10:00 (follow-up)

### Prompt

> update the path and fix inconsistencies in naming

> precommit check config is better kept inside the checks folder instead of
> packages folder

### Prompter

MRDGH2821

### Model

Claude Sonnet 5 via Claude Code

### Actions

- Stopped `nix/devshell.nix` from path-importing the Blueprint-loaded
  `./checks/pre-commit-check.nix` (discouraged by AGENTS.md). The git-hooks
  definition stays in `nix/checks/pre-commit-check.nix`; `devshell.nix` now
  takes the `flake` arg and reads the hook via
  `flake.checks.${system}.pre-commit-check.shellHook` instead of re-importing
  the file. It no longer takes `inputs`. Same change applied to `template/nix/`.
- Fixed stale project name `copier-mr-minimal` -> `copier-mr-nix` (the actual
  repo / remote name) in `flake.nix` description, `README.md` (title + copier
  command), `package.json`, `bun.lock`, `apm.yml` `name`,
  `template/apm.yml.jinja` description, and `template/AGENTS.md` project type.

### Verification

- `nix build .#checks.x86_64-linux.formatting .#checks.x86_64-linux.devshell-default`
  succeeds.
- `devShells.x86_64-linux.default.shellHook` is byte-identical before and after
  the refactor.
- `checks.x86_64-linux` output names are unchanged
  (`pre-commit-check`, `formatting`, `devshell-default`, `pkgs-formatter`).

### Known pre-existing issue (not addressed)

- `checks.x86_64-linux.pre-commit-check` already fails on `HEAD`: the git-hooks
  `typos` hook scans `CHANGELOG.md` and flags short substrings inside commit
  hashes. `typos.toml` has no exclude for `CHANGELOG.md`, unlike the treefmt
  `typos` config which excludes it.

## 2026-08-30T09:10:00+10:00 (follow-up)

### Prompt

> I want to add the following as a recommendation to the end user who will be
> using my template to adopt the given - numtide/prj-spec, dot-config.github.io,
> pi0/config-dir. where should this be put?

### Prompter

MRDGH2821

### Model

Claude Sonnet 5 via Claude Code

### Actions

- Added a "Recommendations -> Configuration directory" section to
  `template/README.md.jinja` and `template/AGENTS.md`, advising that a tool /
  CLI / library generated from this template resolve its own configuration from
  a project-level `.config/` directory, with links to prj-spec,
  dot-config.github.io and pi0/config-dir. Kept in both files because
  `template/AGENTS.md` is excluded when `use_agents` is false.

## 2026-08-30T09:25:00+10:00 (follow-up)

### Prompt

> based on this, can you tell typos to ignore git commit hashes in changelog.md?
> https://github.com/crate-ci/typos/blob/master/docs/reference.md

### Prompter

MRDGH2821

### Model

Claude Sonnet 5 via Claude Code

### Actions

- `typos.toml`: added a custom `[type.changelog]` (glob `CHANGELOG.md`) with an
  `extend-ignore-re` that skips git-cliff commit-hash links
  (`([shorthash](https://.../commit/fullhash))`). Real prose typos in the
  changelog are still caught.
- This fixes the previously-noted `checks.x86_64-linux.pre-commit-check`
  failure; `nix flake check` is now fully green.

### Still open

- `template/nix/checks/pre-commit-check.nix` points the git-hooks `typos` hook
  at `./typos.toml`, but the template ships no `template/typos.toml`. A
  generated project that cuts releases will hit the same CHANGELOG issue.
